SECRET_KEY = django-insecure
DEBUG = False
ALLOWED_HOSTS = *

POSTGRES_PASSWORD = db-password
POSTGRES_HOST = aws.neon.tech
POSTGRES_DATABASE = neondb
POSTGRES_USER = neondb_owner
DB_PORT = 5432
# 'require' for cloud Postgres (Neon etc.); set to 'disable' for local dev Postgres
DB_SSLMODE = require

REDIS_URL = redis://localhost:6379
CACHE_TTL = None

DEFAULT_SUPERUSER_USERNAME = ZLink
DEFAULT_SUPERUSER_EMAIL = zlink@zhiu.dev
DEFAULT_SUPERUSER_PASSWORD = ZLink

# JWT (management API auth)
JWT_ACCESS_MINUTES = 15
JWT_REFRESH_DAYS = 7

# Public base URL of the redirect service (ZLinkClient / Go), e.g. https://zli.nk
# Used to build clickable / copyable short links in the admin UI.
# Leave empty in local dev to fall back to the API host.
SHORT_LINK_BASE_URL =

# Frontend (ZLinkFE) domain(s) allowed to call the API (CORS allow-list).
# Bare domains, comma-separated; each expands to https:// and http://.
# e.g. example.com, test.com   (defaults to the Angular dev server when empty)
FRONTEND_URL =

# Note: GA4 click tracking now lives in the Go client (ZLinkClient), not here.